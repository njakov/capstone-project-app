apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: {{ .Chart.Name }}-secrets
spec:
  provider: gke
  parameters:
    secrets: |
      - resourceName: "projects/{{ .Values.googleProjectId }}/secrets/petclinic-db-username-dev/versions/latest"
        path: "spring.datasource.username"
      - resourceName: "projects/{{ .Values.googleProjectId }}/secrets/petclinic-db-password-dev/versions/latest"
        path: "spring.datasource.password"
      - resourceName: "projects/{{ .Values.googleProjectId }}/secrets/petclinic-db-url-dev/versions/latest"
        path: "spring.datasource.url"
