apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-dashboard
  labels:
    grafana_dashboard: "1"
data:
  # Load the massive JSON file automatically
  petclinic-dashboard.json: |
    {{ .Files.Get "dashboards/4701_rev10.json" | indent 4 }}